# Roadmap

Near-term ideas:
- richer ownership heuristics
- pluggable language backends
- SARIF export
- richer hotspot formulas in config DSL

Current release only promises and implements Dart/Flutter-oriented metrics + git signals + snapshots/trends.
